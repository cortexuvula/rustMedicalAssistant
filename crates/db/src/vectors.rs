//! Vector / embedding storage. Full implementation in Plan 2/3.

pub struct VectorsRepo;

impl VectorsRepo {
    pub fn new() -> Self {
        Self
    }
}

impl Default for VectorsRepo {
    fn default() -> Self {
        Self::new()
    }
}
