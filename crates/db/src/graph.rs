//! Knowledge-graph storage. Full implementation in Plan 2/3.

pub struct GraphRepo;

impl GraphRepo {
    pub fn new() -> Self {
        Self
    }
}

impl Default for GraphRepo {
    fn default() -> Self {
        Self::new()
    }
}
